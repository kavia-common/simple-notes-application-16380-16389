# Base URL for the backend REST API (e.g., http://localhost:8000 or https://api.example.com)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Optional: Used by auth redirect links if backend requires a redirect URL
NEXT_PUBLIC_SITE_URL=http://localhost:3000
